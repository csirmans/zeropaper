# Stage 6: Referee Simulation

**Agents:** `referee` + `referee-freeform` + `referee-mechanism` (launched in parallel — none sees the others' output)

Three referees with distinct mandates read the same paper cold. The structured `referee` gives a top-journal referee report with numbered comments and action tags. The `referee-freeform` gives an editorial publishability assessment — forest, not trees. The `referee-mechanism` evaluates whether the paper's economic mechanism actually delivers the claimed result through the claimed channel (complementary to math-auditor, which checks proof correctness at Gate 2 — mechanism referee checks whether the economics the paper invokes is the economics the math supports).

1. Delete any previous referee reports in `paper/referee_reports/` matching the pattern `*_v*.md` (i.e., `YYYY-MM-DD_vN.md`, `..._vN_freeform.md`, and `..._vN_mechanism.md`). Do **not** delete prior `editor_decision_r*.md` files — the editor reads them as historical context.
2. Launch all three referees in parallel (fresh context, no knowledge of development process). Provide save paths:
   - structured → `paper/referee_reports/YYYY-MM-DD_vN.md`
   - freeform → `paper/referee_reports/YYYY-MM-DD_vN_freeform.md`
   - mechanism → `paper/referee_reports/YYYY-MM-DD_vN_mechanism.md`
3. Commit: `pipeline: stage 6 — referee reports received (structured + freeform + mechanism)`

## Editor aggregation (between referees and Gate 5)

**Agent:** `editor`

The orchestrator does NOT read the three referee reports directly to form a Gate 5 verdict. The `editor` agent is the verdict aggregator — it reads the three reports + paper draft + scorer history + target tier + pipeline state and produces the canonical Gate 5 routing decision. This removes silent-aggregation failures (e.g., a Reject vote from the freeform referee being implicitly outvoted by Major Revision verdicts from the other two).

4. Launch `editor` with:
   - The three referee reports from step 2
   - The current paper draft (`paper/main.tex` + `paper/sections/*.tex`)
   - Pipeline state: `process_log/pipeline_state.json` (editor reads `target_journal_tier`, `referee_round`, `scores`, `regeneration_round`, `seeded`)
   - Glob hint for prior editor decisions: `paper/referee_reports/editor_decision_r*.md`
   - Output path: `paper/referee_reports/editor_decision_rN.md` (N = current `referee_round`)
5. Commit: `artifact: editor decision r{N}`

The editor produces a single file with three load-bearing fields:
   - **Aggregated verdict:** Accept / Minor Revision / Major Revision / Reject — this is what the Gate 5 table below routes on.
   - **Mechanism verdict pass-through:** the mechanism referee's verdict copied verbatim — the editor cannot override it.
   - **Canonical comment list:** the merged, deduplicated, conflict-resolved comment list across all three referees. **Triager runs on this list, not on the raw three reports.**
   - **Journal-fit verdict:** Keep target tier / Downgrade to {tier} / Upgrade to {tier}.

The editor's aggregation rules are spelled out in its agent body (`editor.md`). Summary for orchestrator routing:
- **Mechanism MISATTRIBUTED or DECORATIVE** → editor must set aggregated verdict to Major Revision; mechanism overrides apply downstream as before.
- **Any single Reject vote (structured or freeform)** → editor must set aggregated verdict to Reject. The one allowed escape: editor may downgrade to Major Revision + recommend Downgrade tier IF the rejecting referee's stated reason is journal-fit (not paper quality), with quoted tier-fit language in the justification.
- **Otherwise** → strict aggregation across structured + freeform (the stricter of the two wins).

## Journal-fit handling

If the editor recommends **Downgrade** (one rung down the variant ladder, `{{TIER_LADDER_PROSE}}`):
- Update `target_journal_tier` in `process_log/pipeline_state.json` to the editor's recommended tier (one rung down the ladder above).
- Recompute the Gate 4 advance threshold for the new tier (per the table in `docs/stage_4.md`).
- If the aggregated verdict is **Accept** or **Minor Revision** at the new tier (i.e., the current paper already clears the new threshold per its most recent score), proceed to Stage 7.
- If the aggregated verdict is **Major Revision** (Rule 2 escape from Reject, or Rule 3 + Rule 5 cross-referee tier signal), continue the loop targeting the lower tier.
- **Re-launching Stage 6 after a tier change.** The referees' variant context is baked at deploy time and does not auto-update from `target_journal_tier`. To propagate the new tier, the orchestrator MUST include the following block in each referee's launch prompt on the next round: `> Tier override: the pipeline's active target tier has been updated to {new_tier} (variant tier examples — {{TIER_DOWNGRADE_EXAMPLES}}). Evaluate this paper's fit against that tier, not the original {old_tier} target. The bar for Accept/Minor/Major/Reject must be calibrated to the new tier.` Replace `{new_tier}` and `{old_tier}` with the actual values. Do this for `referee`, `referee-freeform`, and `referee-mechanism` on every round after the tier change, until the pipeline ships.
- Commit: `pipeline: gate 5 — target tier downgraded to {tier} per editor recommendation`.

If the editor recommends **Upgrade** (rare): same procedure with the higher tier, including the same tier-override prompt block on subsequent referee launches.

If the editor recommends **Keep**: no tier change; proceed with the aggregated verdict.

## Gate 5: Referee Decision

Read the editor decision file (`paper/referee_reports/editor_decision_rN.md`). The aggregated verdict + mechanism verdict drive routing per the table below. The triager (when fired) reads the editor's canonical comment list, NOT the raw three referee reports.

**Hard rules driven by the mechanism verdict.** The mechanism verdict interacts with core.md's never-abandon rule (once a paper exists at Stage 5+, the pipeline must produce a finished paper via the deepening playbook, not loop back to Stage 0). The rules below are compatible with never-abandon: neither MECHANISM-MISATTRIBUTED nor MECHANISM-DECORATIVE forces Stage 0. Instead, both route the paper through a mandated restructure-or-narrow path that respects the never-abandon envelope.

- **MECHANISM-VALID** — normal Gate 5 flow.
- **MECHANISM-PARTIAL** — normal Gate 5 flow; the mechanism referee's `[FIX]` items join the other two reports' `[FIX]` items in triage.
- **MECHANISM-MISATTRIBUTED** — forces **Major Revision** as the Gate 5 recommendation, overriding Minor/Accept from the other two referees. The paper must be rewritten around the actual driver the mechanism referee identified. The mechanism report's `[FIX]` items are **locked** — the triager cannot downgrade them under this verdict (see triager rule 3). Revision happens within the never-abandon envelope: the existing paper draft is restructured, not discarded. Deepening playbook applies if the new driver needs added mathematical content.
- **MECHANISM-DECORATIVE** — forces **Major Revision with restructure-or-narrow mandate**, overriding Minor/Accept. DECORATIVE does NOT force Reject, and does NOT route to Stage 0 — both would violate never-abandon. Instead, the theory-generator is given one of two explicit mandates (orchestrator chooses based on content the paper delivers): (a) **restructure** — use the deepening playbook to find real economic content the simple model was hiding, and rebuild the paper's mechanism claim around it; or (b) **narrow** — strip the mechanism framing and present the paper as a structural characterization of the identity the math actually delivers (scientist-first: a correct narrow claim beats a broad decorative one). If after the 10-round cap the verdict remains DECORATIVE, the paper ships with the narrow framing per (b) and a limitations paragraph documenting the original mechanism claim's failure. All mechanism `[FIX]` items are locked (see triager rule 3).

| Aggregated verdict (from `editor_decision_rN.md` — the editor has already applied mechanism overrides in its aggregation, so MISATTRIBUTED/DECORATIVE cannot reach the Accept/Minor row; the bulleted mechanism rules above are reference material) | Action |
|---------------|--------|
| **Accept / Minor Revision** | Fix minor comments per the editor's canonical comment list, proceed to Stage 7 (style check). Only reachable with MECHANISM-VALID or MECHANISM-PARTIAL (the editor enforces this — it sets Major Revision under MISATTRIBUTED/DECORATIVE regardless of the underlying referees). |
| **Major Revision / Revise and Resubmit** | **Triage first.** Launch `triager` with: inputs = the editor's canonical comment list from `paper/referee_reports/editor_decision_rN.md` + the three raw referee reports (for the verbatim comment text and the `## What would be publishable` sections, which the editor does not duplicate), output path = `paper/referee_reports/triage_rN.md` (N = `referee_round`), context = `gate-5`. The triager treats the editor's canonical comment list as the authoritative concern list — it does not re-aggregate or re-deduplicate; it applies the classification rules (no silent downgrade of referee `[FIX]`, written justifications for downgrades, mechanism-report `[FIX]` items under MECHANISM-MISATTRIBUTED or MECHANISM-DECORATIVE are locked per triager rule 3) and judges each round on its merits. Do not edit the triager's output — if you disagree, re-launch with explicit instructions, do not silently override. Then revise only the `[FIX]` items. Under MISATTRIBUTED, the revision must rewrite around the actual driver. Under DECORATIVE, pick the restructure-or-narrow path from the rules above and pass the chosen mandate to the theory-generator / paper-writer. When a referee challenges an assumption, first try to prove the result without it or characterize exactly when it fails — weakening claims is the last resort (per "characterize, don't just prove"). **Increment `referee_round` in `pipeline_state.json` before re-running Stage 6.** Re-run Stage 6. Max 10 rounds (i.e., stop when `referee_round >= 10`); keep going as long as the triager reports any `[FIX]` items in the current round (the triager is stateless — it does not distinguish "new" from "carried forward"; any `[FIX]` indicates outstanding work). **At cap (`referee_round >= 10` with `[FIX]` items still outstanding):** proceed to Stage 7 with the best available draft and have paper-writer add a single limitations paragraph enumerating the unresolved `[FIX]` items from `triage_r10.md`. Never-abandon prevails; do not loop further at Stage 6. |
| **Reject** | Stage 6 only fires post-Stage-5 by construction, so a paper draft always exists; never-abandon applies. MECHANISM-DECORATIVE / MISATTRIBUTED do NOT route here — the editor sets Major Revision under those verdicts regardless of structured/freeform recommendations. So the Reject row is reached only when the mechanism verdict is VALID or PARTIAL **and** the editor's aggregated verdict is Reject (i.e., at least one referee said Reject and the editor did not invoke the journal-fit downgrade escape). **Triage first** (same launch as Major Revision above; inputs = editor canonical list + raw referee reports for the `## What would be publishable` sections) — the triager applies rule 3a and produces a `## Deepen directive (Reject)` block at the top of `triage_rN.md` synthesized from each rejecting referee's `## What would be publishable` section. **Verify the triage file's Summary contains no `triage error` entries before proceeding** — if any rejecting referee failed to produce a `## What would be publishable` section, re-launch that referee with explicit instruction to produce it, then re-run the triager. **The deepen directive is the canonical input on this round; `[FIX]` items in the triage table are deferred — in practice, the next round's referees re-read the deepened paper cold and will re-flag any of those issues that the deepening did not resolve, so deferral is recovery-by-re-detection, not a tracked queue.** Pass the deepen directive to theory-generator (and empiricist if `--ext empirical` — re-fire Stage 3a per `docs/stage_3a_empirical.md` "Re-fire on theory revision" with the directive's empirics requirements as the focus; and experiment-designer if `--ext theory_llm` — re-fire Stage 3b per `docs/stage_3b_experiments.md` "Re-fire on theory revision" with the directive's experimental requirements as the focus). The mandate is **deepen the core idea, do not extend the surface area** — prove a sharper version of the main result, characterize boundary behavior, surface a mechanism the simple model hid, run the test that actually disciplines the claim. Adding extensions, robustness legs, or scope conditions is NOT the response to Reject (those are Major Revision responses). After theory-generator/empiricist produce the deepened version, **launch `branch-manager` with context = `gate-5-reject`, output path `paper/referee_reports/branch_manager_reject_rN.md` (N = `referee_round`), and inputs = the deepen directive + `paper/referee_reports/editor_decision_rN.md` + diff (theory_draft_v(N-1) → v(N), plus the empirical_analysis diff if applicable) + `process_log/pipeline_state.json` (branch-manager reads `reject_cosmetic_round` from it). Branch-manager produces Section A only and emits a substantive-vs-cosmetic verdict on the deepening.** If COSMETIC (rename / restate / reorganize / scope conditions added without a new mechanism / proof / characterization / test): increment `reject_cosmetic_round` in `pipeline_state.json`. On first cosmetic verdict (`reject_cosmetic_round == 1`), force one more attempt with feedback "the previous response was cosmetic; deepen substantively." On second consecutive cosmetic verdict (`reject_cosmetic_round == 2`), treat as theory failure: if `regeneration_round == 0` and not seeded, route through the **Regeneration Round** protocol (see `docs/stage_1.md` "Regeneration round"). Procedure: (i) re-launch `branch-manager` with context = `gate-5-reject-regen`, output path = `output/stage1/learnings_r1.md`, inputs = the deepen directive + theory draft history + `stage1_candidates` + prior gate-5-reject branch-manager reports; the agent produces only the learnings file (no main report). (ii) Archive the paper to `paper_archive/r1/`. (iii) Increment `regeneration_round` to 1, reset `theory_attempt` and `theory_version` to 1, reset `reject_cosmetic_round` to 0, **reset `referee_round` to 0** (the regenerated paper gets its own 10-round Stage 6 budget), reset `fix_empirics_round` to 0, reset `stage2b_theory_version` to `null`, and reset `triaged_lit_implications` to `[]`. (iv) Re-enter Stage 1 with the learnings file passed to idea-generator and idea-reviewer per the bullets in `docs/stage_1.md` "Regeneration round". If a regeneration has already fired (`regeneration_round > 0`) or the run is seeded, fall back to standard Major Revision treatment (deepening-playbook mandate, never-abandon) and reset `reject_cosmetic_round` to 0 — the deepen path has been exhausted; the paper ships under the regular 10-round cap. If SUBSTANTIVE: reset `reject_cosmetic_round` to 0, increment `referee_round`, re-run Stage 6. Max 10 rounds (counted on `referee_round`, same as Major Revision). |

{{SEED_OVERRIDE_STAGE_6_GATE_5}}
